<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tiger.crm.repository.mapper.TicketMapper">

    <!-- 티켓 리스트 쿼리 -->
    <select id="getTicketList" resultType="map"  parameterType="string">
        SELECT TICKET_ID, COMPANY_ID, COMPANY_NAME,STATUS_CD,TITLE
        FROM T_TICKET_INFO
        <if test='searchKeyword != null'>
            WHERE TITLE LIKE CONCAT('%', #{searchKeyword}, '%')
            OR CONTENT LIKE CONCAT('%', #{searchKeyword}, '%')
        </if>
    </select>

    <!-- 티켓 리스트 검색 쿼리 -->
    <select id="searchTickets" resultType="map">
        SELECT TICKET_ID, COMPANY_ID, COMPANY_NAME,STATUS_CD,TITLE FROM tickets
        WHERE TITLE LIKE CONCAT('%', #{searchKeyword}, '%')
        OR CONTENT LIKE CONCAT('%', #{searchKeyword}, '%')
    </select>

</mapper>
